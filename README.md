# pyEWMACD
This is an early (but working) python implementation of 

# Example run
out = EWMACD(ds=ds, 
             trainingPeriod='static',
             trainingStart=2000,
             trainingEnd=2005,
             persistence_per_year=1)
